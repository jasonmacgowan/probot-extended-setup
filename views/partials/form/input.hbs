<div class="form-group">
  <div class="form-group-header">
    <label for="{{env}}">{{name}}</label>
  </div>
  <div class="form-group-body">
    <input class="form-control" type="{{#if type}}{{type}}{{else}}text{{/if}}" name="{{env}}" value="{{value}}" />
  </div>
</div>